name: desktop downloaded acceptance
on:
  workflow_dispatch:
    inputs:
      archive_url:
        required: true
        type: string
      archive_sha256:
        required: true
        type: string
      version:
        required: true
        type: string
permissions:
  contents: read
  actions: read
jobs:
  minimum-os:
    runs-on: macos-14
    timeout-minutes: 25
    env:
      ARCHIVE_URL: ${{ inputs.archive_url }}
      ARCHIVE_SHA256: ${{ inputs.archive_sha256 }}
      RELEASE_VERSION: ${{ inputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
      - run: npm run build
      - run: node node_modules/electron/install.js
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: whipcode-ci-renderer
          path: apps/web
          run-id: 34295395906
          github-token: ${{ github.token }}
      - name: Load the release renderer reference for archive verification
        run: |
          node --input-type=module <<'NODE'
          import assert from 'node:assert/strict';
          import { readFile } from 'node:fs/promises';
          const manifest = JSON.parse(await readFile('apps/web/renderer-manifest.json'));
          assert.equal(manifest.source.commit, '2cea57c23b138284974226e9175d8bf1d8c9ba97');
          assert.equal(manifest.source.dirty, false);
          NODE
          node scripts/pack-web.mjs
      - name: Download and verify the exact published archive
        run: node apps/desktop/scripts/download-acceptance.mjs
      - name: Launch the notarized app on the minimum supported macOS
        run: node apps/desktop/scripts/startup.mjs 'downloaded/Whip Beta.app' --notarized --samples 30 --first-samples 1 --output acceptance/minimum-os-startup.json
      - name: Require the expected OS, version, and successful cleanup
        run: |
          node --input-type=module <<'NODE'
          import assert from 'node:assert/strict';
          import { readFile } from 'node:fs/promises';
          import { execFileSync } from 'node:child_process';
          const os = execFileSync('/usr/bin/sw_vers', ['-productVersion'], { encoding: 'utf8' }).trim();
          assert(os.startsWith('14.'));
          assert.equal(process.arch, 'arm64');
          const evidence = JSON.parse(await readFile('acceptance/minimum-os-startup.json'));
          assert.equal(evidence.evidence.version, process.env.RELEASE_VERSION);
          assert(evidence.completed && !evidence.interrupted);
          assert(evidence.cleanup.every(value => value.state === 'stopped'));
          console.log(`Verified ${process.env.RELEASE_VERSION} on macOS ${os} arm64`);
          NODE
      - name: Grant the disposable CI helper permission to click the owned update dialog
        run: |
          xcrun swiftc apps/desktop/scripts/click-button.swift -o "$RUNNER_TEMP/whip-click-button"
          codesign --force --sign - "$RUNNER_TEMP/whip-click-button"
          sudo python3 - "$RUNNER_TEMP/whip-click-button" <<'PYTHON'
          import os,sqlite3,sys,time
          assert os.environ.get('SUDO_USER') == 'runner'
          database='/Library/Application Support/com.apple.TCC/TCC.db'
          connection=sqlite3.connect(database)
          connection.execute('INSERT OR REPLACE INTO access (service,client,client_type,auth_value,auth_reason,auth_version,indirect_object_identifier_type,indirect_object_identifier,flags,last_modified) VALUES (?,?,?,?,?,?,?,?,?,?)', ('kTCCServiceAccessibility',sys.argv[1],1,2,4,1,0,'UNUSED',0,int(time.time())))
          connection.commit()
          PYTHON
          "$RUNNER_TEMP/whip-click-button" --check
      - name: Exercise an actual signed Squirrel update and canonical backend handoff
        if: inputs.version != '0.2.0-beta.0'
        run: node apps/desktop/scripts/squirrel-acceptance.mjs
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        if: always()
        with:
          name: desktop-download-acceptance
          path: acceptance
          if-no-files-found: error
          retention-days: 30
