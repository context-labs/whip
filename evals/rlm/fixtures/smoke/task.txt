Find the unique RLMSMOKE token without loading the full corpus into root context. In rlm_exec, search the smoke-corpus context handle for the prefix RLMSMOKE-, take the first match's start offset, and read a bounded excerpt from that position. Identify the complete RLMSMOKE-NNNN token and its exact corpus byte span. Return exactly one line in this format, replacing each placeholder with the observed evidence: result=<token> citation=smoke-corpus:<start>-<end>. The end offset is exclusive and covers only the token.
