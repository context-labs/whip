The repository migration inventory is the comparison-corpus context handle named in your system instructions. In one execution cell, call models.batch once with two stateless reviewer prompts, search comparison-corpus for migration_target=true, then read the matching record through context.read. Return an object containing the exact matching text, its returned handle, and the exact byte span covering migration_target=true through the end of its value token (excluding the newline). Do not call files operations, do not copy the corpus into root context, and do not guess if the context handle is unavailable. Your final assistant response must be exactly one line in this format, using the observed value token and byte span: result=<value> citation=comparison-corpus:<start>-<end>. The end offset is exclusive.
