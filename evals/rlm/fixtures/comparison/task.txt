The repository migration inventory is the `comparison-corpus` context handle named in your system instructions. In one Starlark cell, call `models.batch` once with two stateless reviewer prompts, call `context.search(handle="comparison-corpus", query="migration_target=true")`, then return a dictionary containing the exact matching text and its returned handle/span citation. Do not call `files.*`, do not copy the corpus into root context, and do not guess if the context handle is unavailable.
