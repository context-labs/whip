Fix delay() in /app/backoff.py. attempt is the number of completed failures: attempt=0 returns zero, attempt=1 returns base, then the delay doubles for each further failure up to cap. Reject negative or noninteger attempts (including bool), and reject base or cap values that are not finite positive numbers (including bool). Huge attempts must return cap without constructing huge integers. Preserve the function signature, add useful tests, run them, and commit your changes in the task repository. No human is available.
