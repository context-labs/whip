# Modal Docker eval execution

Branch: feature/agent-definition (existing checkout; do not switch or discard unrelated work)

## Goal and authorization
User approved end-to-end implementation, provisioning, paid qualification and cleanup on 2026-09-11, while away for several hours. Target reliable frequent CLI-first 30-task Full x 3 repetitions, up to 90 simultaneous attempts. Modal preferred; real Docker containers required. Model-provider capacity user-confirmed arranged. Throughput first, no hard spending cap. Root bounds initial delivery to qualification plus ONE new 90-attempt campaign after gates; no automatic replacement campaign or baseline promotion.

## Non-goals
No dashboard, scheduling service, general sandbox abstraction, Go agent/runtime redesign, benchmark-content changes, hidden timeout/retry changes, or automatic task replay after uncertain execution. Do not touch unrelated Modal resources, main environment, token/account settings, or existing historical artifacts. Never print credentials or put credentials in images, bundles, logs, Git or reports.

## Verified starting point
Modal profile/workspace inference-net authenticated. whipcode is an ENVIRONMENT, not an app; explicit environment targeting required because main is default. Read-only environment/apps/volumes/secret-metadata access works; whipcode listings empty on 2026-09-11. Writes, VM beta and quota not yet verified. Working tree clean before this plan. Existing pinned Harbor0.22.0, Pier0.3.1 and Modal1.5.5 provide reusable tools. Both runners have native Modal adapters, but preserve Docker execution first.

## Architecture
CLI submits immutable prepared campaign bundle to a detached remote coordinator. Each task-repeat gets a fresh Modal VM Sandbox running the EXISTING native Docker trial worker, adapter, observer and grader. Preserve shared terminal verifier and separate repository verifier + inference-only proxy networking. No host Docker socket or Modal credentials in agent task containers. Remote coordinator and per-trial execution identities persist outside disposable workers; worker exports phase and final evidence independently of CLI/coordinator connection. One owner coordinates dispatch; uncertain creation is reconciled by identity, never blindly retried. Client exit is harmless; controller crash can reconcile IDs and collect completed workers, but dead model sessions are not claimed resumable. Preserve interrupted attempts and costs.

Use small additions to evals/whip_evals, not a second eval platform. Reuse prepare.py, execution.py job_config/execute_job, report.py and existing schema where possible. Add a narrow Modal worker/coordinator/storage module and CLI submit/status/logs/fetch/cancel. Explicit backend execution identity and cloud concurrency limit may exceed existing local32 cap, without changing native timing/scoring. Immutable manifest records task+binary+image+runner+model+backend/resource/network/cache provenance. Keep native Docker config and custom PierDockerEnvironment fix.

## Resources and lifetime
Terminal tasks: 1 logical Docker CPU, usually2GiB RAM, two4GiB; task storage reservation10GiB. Repository agent2CPU/8GiB plus separate verifier2CPU/8GiB; whole-trial reservation4CPU/16GiB/40GiB before host overhead. Modal CPU units are physical cores, NOT Docker logical/CFS CPU quota; validate conversion/cgroups. VM RAM static: include proxy/kernel/runner/Docker/page cache. Disk needs image layers/logs beyond task reservation (not assumed enforced quota). Existing max outer envelope13066s plus outer boot/export/cleanup allowance, not merely5400s agent deadline. Preserve native inner hard caps and deadlines. Use requested resource billing while model waits; report infra separately from model costs/unknowns.

## Setup fidelity
Initially pin/cache worker/tool images and original task OCI images; leave adapter.setup and timed boundaries intact. Capture setup subcommand evidence and fix misattribution in reporting. Prebaking agent dependencies changes setup protocol; if required for reliable operation, implement as separately identified and qualified prepared-image policy, explicitly document deviation, keep task contents/tests/model/grading unchanged. No hidden retries or timeout increases. Different backend remains new environment, not assertion of baseline equivalence.

## Data and security
Default intent raw evidence30days, compact manifests/reports longer; do not delete historical evidence or claim a retention policy is enforced without implementation. Workspace-restricted durable storage. Stage outputs incrementally with atomic completion markers and durable owned sandbox IDs. Provider credentials transferred via secure Modal Secret workflow using existing authorized local provider configuration, never copied wholesale. Resource names prefix whip-eval, explicit whipcode environment, owner/campaign labels and TTLs. Cleanup removes only proven-owned resources; retain intentional deployment/storage/secret and document them. No blanket workspace cleanup. Keep external resource ledger and scrubbed cost/evidence receipts.

## Ordered delivery and gates
- [x] User questions, vendor/current-runner research, authentication read checks.
- [x] Final living plan and scoped execution authorization recorded.
- [x] Infrastructure qualification PASS: fourVMs ended, qualification app stopped; Docker28.3.3/Compose2.39.2 bridge/DNS/internal networking, cgroups, OOM, signals, UNIX-only daemon/no innergatewayAPI and filetransfer passed. Immutable Debian/Python3.12.10 base im-jUQGZWajkRbfGPY11rNEN3,6CPU/24GiB admitted. Creator disconnect/liveVolumeAPI readback and SIGKILL lastconfirmed hashretention passed (5.43–9.37s visibilitylag, not lossless). VM volume reload unsupported/boot-snapshot: external ACK/cancel must use directcontrol path, not unseenVolumewrites. Report evals/artifacts/modal-qualification-20260911/QUALIFICATION.md. Ownership transferred qualifier→implementation. Provisional appfiltered$0.03934712; finalunknown; modelcalls0. Nativefixtures/90way remain separate gates.
- [x] Implementation and focused tests: CLI submit/status/logs/fetch/cancel/reconcile, immutable bundles, exact ownership, partial accounting, native/VM cleanup separation, and bounded host collection implemented. Full Python suite89 PASS; reviewer independently ran44 Modal tests. Controller remains796412002c8cf0e0da9aab1bb0a59f063b3e8a7407ed21aba25ebc6607db171a. Collector uses pinned Modal1.5.5 private streaming helper with8 bounded logical downloads; no global HTTP/memory or complete SDK background-cancellation claim.
- [x] Native fixture qualification BOTH runners: E2b independently passed both graders,114/114 final hashes and44 durable inspector readback comparisons; actual Pier proxy needle captured privately,0 matches/errors in closed artifacts. Network/cgroup/verifier separation qualified within documented probe scope. E3 completed natively: prior partial/stale-progress assertions explicitly superseded. F1 interrupted unfinished native work and retained truthful PARTIAL evidence,47/47 hashes and exact pre-termination event prefix. Accept observed platform-termination/partial-recovery behavior, NOT instantaneous crash or provider-request interruption. F1 strict30s exit bound FAILED/raw INCONCLUSIVE remains: actual exit137 observed30.473s after first request, after a second cleanup request; wrapper finalized before exit. No extra fault attempts remain authorized.
- [x] 90-way credential-free lifecycle/admission qualification: modal-native-fixture-20260911-90b, canonical e9/75b binary,45 Harbor+45 Pier native fixtures,90/90 graded successes. Independently verified90 owned VM overlap (not CPU/model-work overlap),90 actual exits and post-terminal90-key reconciliation. All5130 final file hashes/5220 retained files verified;450 fake loopback calls,0 external-provider calls, complete fake-model accounting. Infrastructure cost unknown. Fresh4680-file integrity/security scan had no errors; no exact-key scan was performed in this final audit. Immutable closure SHA bc62599846bf7f08aeae2314187e25d3d6c55cf580943a7b0ca924b2751a894b. Follow-up timer canceled after final audit.
- [x] Independent adversarial review through credential-free qualification: source, pinned SDK behavior, frozen input identity, runtime ownership/overlap, actual-exit cleanup, native grades, complete inventories and accounting verified. Live-input/credential reviews remain separate gates; unrelated repository-wide checks are not claimed.
- [ ] Small live-model smoke: exact2 selected, one terminal-bench/openssl-selfsigned-cert and one datacurve/httpx-multipart-response-parsing, one attempt each outside future90. OFFLINE preparation only as of2026-09-11T20:50Z; no credential provisioning/provider requests/live dispatch authorized. Preserve original task bytes, model/catalog/QuickJS/high effort, native900/900 and5400/1800 deadlines and native verifier retries; no fixture holds. Healthy graded FAIL does not trigger replay. Separate exact2 monitor must cover original13066s maximum outer envelope; root reviews frozen bundle, secure Secret plan and readiness before launch.
- [ ] ONE 30tasks x3repetitions campaign, frozen behavior, up to90 concurrent. Observe actual peak; if capacity forces lower concurrency report deviation, never pretend90 concurrent. Exactly90 planned attempts; no silent replacement for failed/unknown attempt.
- [ ] Final evidence/accounting/provenance/cleanup audit, docs, resource inventory and operational runbook. Report all attempted/graded/ungraded/unknown, observed throughput and real costs. No automatic accepted-baseline promotion.

## Ownership and implementation surfaces
Root owns plan, integration review, overall spending and full-campaign launch authorization.
Implementation lead owns evals/whip_evals, evals/tests, necessary protocol/dependency changes and evals/README/docs/features/docs/roadmap updates. It may delegate disjoint test/docs work, but coordinates source ownership.
Infrastructure qualifier owns only new ignored evals/artifacts/modal-qualification-20260911 evidence/scripts and cloud resources during qualification. It sends explicit ownership handoff before implementation or campaign workers mutate the same remote resources. Initial qualification may provision resources and build images, but no real-model calls.
Expected source files: cli.py, run.py, execution.py, prepare.py, doctor.py, report.py, small modal module(s); adapter.py only targeted setup/evidence seams if needed. Preserve task lock absent explicit prep identity change. Docs: evals/README.md, docs/features.md, relevant docs/roadmap entry and this plan. No commits/push without final intentional diff/secret review.

## Research and source anchors
- evals/whip_evals/execution.py:93-114 Docker native config, :130-178 process execution, :195-228 outer envelope, :231-289 owned cleanup.
- evals/whip_evals/adapter.py:92-106 Pier Squid fix, :142-173 network/setup, :187-224 observer launch.
- evals/frontier/tasks.lock.json and tasks.py:159-176 frozen images and agent+verifier reservations; all30 tasks have no CUSTOM Compose, but native Docker runners use Compose internally.
- evals/artifacts/setup-20260910/BASELINE-COHORT-FINAL-REPORT.md: historical90 attempts85grades48pass37fail5setup; known$334.362495, totalunknown; not cloud qualification.
- https://modal.com/docs/guide/vm-sandboxes Docker supported on real-kernel VM beta, limitations require actual checks.
- https://modal.com/docs/guide/sandbox-resources and https://modal.com/pricing rates/units (accessed2026-09-11).
- https://modal.com/docs/guide/sandbox-lifecycle detached lifetime and cleanup; native SDK not itself durable campaign logic.
- Public vendor research artifact df0daeb442bb385cb7381aaf4cc1ecd0; E2B viable fallback but standard resource shape insufficient for conservative whole Pier worker without custom sizing.

## Progress/deviations
2026-09-11: Initial plan finalized. Implementation lead sandbox-eval-architecture and cloud qualification owner sandbox-modal-research started. Independent reviewer modal-eval-review supplied concrete gates: allowlisted authenticated bundle/no control-plane credentials in task scope; persisted create intent and duplicate reconciliation; exact owned cleanup; worker-independent incremental evidence; unchanged image-pull/native timing boundaries; real network/cgroup/verifier probes. Root accepted these gates; no model campaign launched. First real gate is VM/Docker availability. If unavailable after a bounded investigation, qualify native Modal adapters or another Docker-capable approach rather than loop; notify root with explicit fidelity/resource implications.
