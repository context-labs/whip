# Canonical whipcode implementation and cutover

Branch: `whip-rlm` in the primary checkout.

The [approved plan](../canonical-whipcode-installation.md) records scope,
architecture, cleanup inventory and acceptance requirements. The user approved
execution end to end, including discarding historical local Whip state and
retaining desktop with the canonical whipcode backend.

- [x] Implement canonical desktop executable selection and explicit installation.
- [x] Package a real whipcode payload with identity and build provenance.
- [x] Add read-only diagnostics, UI recovery and cancellation behavior.
- [x] Make CLI daemon status read-only and preserve explicit remote-home overrides.
- [x] Focused native, UI, packaging and Go race checks; independent review fixes.
- [ ] Full repository check and signed/notarized package.
- [ ] Isolated installed-runtime, real desktop and worker-continuity acceptance.
- [ ] Stop old local runtimes and remove confirmed retired installation/state.
- [ ] Install and bootstrap the canonical CLI, desktop and fresh home.
- [ ] Record final process/path/build verification and remaining user actions.

No new dependencies were introduced. The existing SDK, shared host dialog,
native bridge, Go owner lock, build-overlay signing and packaging verification
remain the implementation boundaries. New tests live in the existing native,
app and Go suites. Evidence generated by this execution is under `evidence/`.
