import { Stack as RouterStack } from 'expo-router';
import { Linking } from 'react-native';
import { Button, Screen, Section, Text } from '../ui';
export default function SetupHelp() { return <><RouterStack.Screen options={{ title: 'Connect a host' }} /><Screen><Text variant="title">Your computer does the work.</Text><Text muted>Whip mobile connects to an existing Whip host. Sessions and provider credentials stay on that computer.</Text><Section title="1 · JOIN YOUR PRIVATE NETWORK"><Text>Connect your phone and computer to the same Tailscale network.</Text></Section><Section title="2 · START WHIP ON YOUR COMPUTER"><Text>Use Whip’s private HTTPS listener setup. Tailscale Serve forwards your private HTTPS address to Whip; it should remain accessible only to your tailnet.</Text></Section><Section title="3 · ADD THE HOST"><Text>Enter the HTTPS base address in Whip mobile, then Test Connection. Don’t add an API path. An empty session list is a successful connection.</Text></Section><Button label="Tailscale Serve documentation" variant="secondary" onPress={() => { void Linking.openURL('https://tailscale.com/docs/features/tailscale-serve'); }} /><Text muted>If the host has no configured provider, finish provider setup on the computer before sending a message.</Text></Screen></>; }
