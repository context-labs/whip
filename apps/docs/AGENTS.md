Read ../../docs/frontend.md (Public documentation site) and ../../docs/brand-guide.md before changing this app. The docs site owns its components and styling: component styles are StyleX (co-located `*.stylex.ts`, docs-local tokens in `src/tokens.stylex.ts`), with plain CSS reserved for global base rules, token custom properties, and MDX prose elements. It never imports product SDK, app or UI packages. MDX under src/content/docs is trusted executable source. Preserve the static-only deployment contract in README.md; test generated HTML with JavaScript disabled. Run root check:docs, test:docs, build:docs, test:docs:browser and build:storybook:docs.
