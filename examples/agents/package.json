{
  "name": "@whip/agents-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && node --test dist/*.test.js",
    "acceptance": "npm run build && node --test --test-concurrency=1 support-triage.acceptance.mjs incident-commander.acceptance.mjs"
  },
  "dependencies": {
    "@whip/sdk": "0.1.0",
    "zod": "^4.5.4"
  }
}
